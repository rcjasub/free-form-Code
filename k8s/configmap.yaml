# ConfigMap — non-sensitive environment variables for the backend
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  PORT: "4000"
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: freeformcode
  DB_USER: postgres
  REDIS_URL: redis://redis:6379
  CLIENT_URL: http://localhost
